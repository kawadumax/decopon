[package]
name = "decopon-app-ipc"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
decopon-axum = { path = "../../../backend/axum" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
tower = { version = "0.5", features = ["util"] }
